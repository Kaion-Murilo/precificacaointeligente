@{
    ViewData["Title"] = "Home Page";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

<style>
    /* ======== ESTILO GLOBAL ======== */
    body {
        background: linear-gradient(135deg, #0d6efd, #00bfa6);
        font-family: 'Segoe UI', sans-serif;
        color: #fff;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        margin: 0;
        overflow-x: hidden;
    }

    h1 {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 1rem;
    }

    p.lead {
        font-size: 1.2rem;
        max-width: 700px;
        margin: 0 auto 2rem;
        line-height: 1.6;
    }

    .btn-gradient {
        background: linear-gradient(135deg, #ffffff, #e0f7f4);
        color: #0d6efd;
        font-weight: 600;
        font-size: 1.1rem;
        padding: 0.9rem 2.5rem;
        border: none;
        border-radius: 50px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        transition: all 0.3s ease;
    }

        .btn-gradient:hover {
            transform: translateY(-3px);
            background: linear-gradient(135deg, #eafaff, #b3f3e9);
        }

    .icon-feature {
        font-size: 3rem;
        color: #fff;
        margin-bottom: 1rem;
    }

    .features {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 2rem;
        margin-top: 3rem;
    }

    .feature-box {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        padding: 2rem;
        width: 250px;
        backdrop-filter: blur(6px);
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
        transition: 0.3s;
    }

        .feature-box:hover {
            transform: translateY(-8px);
            background: rgba(255, 255, 255, 0.2);
        }

</style>

<!-- ======= CONTEÚDO PRINCIPAL ======= -->
<div class="container text-center">
    <h1>💼 Sistema de Precificação Inteligente</h1>
    <p class="lead">
        Uma plataforma simples e poderosa para calcular custos, margens de lucro e ponto de equilíbrio — tudo com base em equações lineares reais e gráficos interativos.
    </p>
    <a href="/login/Index" class="btn btn-gradient">
        🚀 Acessar o Sistema
    </a>


    <div class="features mt-5">
        <div class="feature-box">
            <i class="bi bi-calculator icon-feature"></i>
            <h5>Precificação Automática</h5>
            <p>Calcule lucros e preços ideais com base em dados reais.</p>
        </div>

        <div class="feature-box">
            <i class="bi bi-graph-up-arrow icon-feature"></i>
            <h5>Equações Lineares</h5>
            <p>Visualize e entenda a matemática por trás dos cálculos.</p>
        </div>

        <div class="feature-box">
            <i class="bi bi-bar-chart icon-feature"></i>
            <h5>Gráficos Dinâmicos</h5>
            <p>Veja a relação entre custo, receita e lucro em tempo real.</p>
        </div>

        <div class="feature-box">
            <i class="bi bi-lightbulb icon-feature"></i>
            <h5>Aprendizado Inteligente</h5>
            <p>Entenda como otimizar seus resultados usando matemática aplicada.</p>
        </div>
    </div>
</div>
